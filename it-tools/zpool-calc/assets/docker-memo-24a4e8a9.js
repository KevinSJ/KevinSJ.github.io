import{o as n,c as t,P as c,d as p,bY as i,l as a,cf as d,k as l,bq as g}from"./index-0c1fde36.js";const h={class:"markdown-body"},u=c(`<p>Docker provides the ability to package and run an application in a loosely isolated environment called a container. The isolation and security allows you to run many containers simultaneously on a given host. Containers are lightweight and contain everything needed to run the application, so you do not need to rely on what is currently installed on the host. You can easily share containers while you work, and be sure that everyone you share with gets the same container that works in the same way.</p><h2>INSTALLATION</h2><h2>GENERAL COMMANDS</h2><p>Docker Desktop is available for Mac, Linux and Windows <a href="https://docs.docker.com/desktop">https://docs.docker.com/desktop</a></p><p>View example projects that use Docker</p><p><a href="https://github.com/docker/awesome-compose">https://github.com/docker/awesome-compose</a></p><p>Check out our docs for information on using Docker <a href="https://docs.docker.com">https://docs.docker.com</a></p><h2>IMAGES</h2><p>Docker images are a lightweight, standalone, executable package of software that includes everything needed to run an application: code, runtime, system tools, system libraries and settings.</p><p>Build an Image from a Dockerfile</p><pre><code class="language-bash">docker build -t &lt;image_name&gt; .
</code></pre><p>Build an Image from a Dockerfile without the cache</p><pre><code class="language-bash">docker build -t &lt;image\\_name&gt; . -no-cache
</code></pre><p>List local images</p><pre><code class="language-bash">docker images
</code></pre><p>Delete an Image</p><pre><code class="language-bash">docker rmi &lt;image\\_name&gt;
</code></pre><p>Remove all unused images</p><pre><code class="language-bash">docker image prune
</code></pre><h2>DOCKER HUB</h2><p>Docker Hub is a service provided by Docker for finding and sharing container images with your team. Learn more and find images at <a href="https://hub.docker.com">https://hub.docker.com</a></p><p>Login into Docker</p><pre><code class="language-bash">docker login -u &lt;username&gt;
</code></pre><p>Publish an image to Docker Hub</p><pre><code class="language-bash">docker push &lt;username&gt;/&lt;image\\_name&gt;
</code></pre><p>Search Hub for an image</p><pre><code class="language-bash">docker search &lt;image\\_name&gt;
</code></pre><p>Pull an image from a Docker Hub</p><pre><code class="language-bash">docker pull &lt;image\\_name&gt;
</code></pre><p>Start the docker daemon</p><pre><code class="language-bash">docker -d
</code></pre><p>Get help with Docker. Can also use -help on all subcommands</p><pre><code class="language-bash">docker --help
</code></pre><p>Display system-wide information</p><pre><code class="language-bash">docker info
</code></pre><h2>CONTAINERS</h2><p>A container is a runtime instance of a docker image. A container will always run the same, regardless of the infrastructure. Containers isolate software from its environment and ensure that it works uniformly despite differences for instance between development and staging.</p><p>Create and run a container from an image, with a custom name:</p><pre><code class="language-bash">docker run --name &lt;container\\_name&gt; &lt;image\\_name&gt;
</code></pre><p>Run a container with and publish a container’s port(s) to the host.</p><pre><code class="language-bash">docker run -p &lt;host\\_port&gt;:&lt;container\\_port&gt; &lt;image\\_name&gt;
</code></pre><p>Run a container in the background</p><pre><code class="language-bash">docker run -d &lt;image\\_name&gt;
</code></pre><p>Start or stop an existing container:</p><pre><code class="language-bash">docker start|stop &lt;container\\_name&gt; (or &lt;container-id&gt;)
</code></pre><p>Remove a stopped container:</p><pre><code class="language-bash">docker rm &lt;container\\_name&gt;
</code></pre><p>Open a shell inside a running container:</p><pre><code class="language-bash">docker exec -it &lt;container\\_name&gt; sh
</code></pre><p>Fetch and follow the logs of a container:</p><pre><code class="language-bash">docker logs -f &lt;container\\_name&gt;
</code></pre><p>To inspect a running container:</p><pre><code class="language-bash">docker inspect &lt;container\\_name&gt; (or &lt;container\\_id&gt;)
</code></pre><p>To list currently running containers:</p><pre><code class="language-bash">docker ps
</code></pre><p>List all docker containers (running and stopped):</p><pre><code class="language-bash">docker ps --all
</code></pre><p>View resource usage stats</p><pre><code class="language-bash">docker container stats
</code></pre>`,59),m=[u],k={__name:"docker",setup(r,{expose:e}){return e({frontmatter:{}}),(s,b)=>(n(),t("div",h,m))}},_=p({__name:"docker-memo",setup(r){i(o=>({c4cf6bc0:a(e).cardColor,"5d0dfa6e":a(e).textColor1}));const e=d();return(o,s)=>(n(),t("div",null,[l(a(k),{style:{"overflow-x":"auto"}})]))}});const w=g(_,[["__scopeId","data-v-18599112"]]);export{w as default};
